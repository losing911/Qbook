# CyberPanel / OpenLiteSpeed Rewrite Rules
# Add this to: Websites > List Websites > Manage (for qbook.anxipunk.icu) > Rewrite Rules

RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
REWRITERULE ^(.*)$ http://127.0.0.1:3001/$1 [P]
